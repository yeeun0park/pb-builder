# POS 모드 구현 계획서

> **에이전트 작업자에게:** 이 계획서는 `superpowers:subagent-driven-development`(추천) 또는 `superpowers:executing-plans`로 단계별 실행. 각 단계는 체크박스(`- [ ]`)로 진행 추적.

**목표:** pb-builder에 "POS 모드"를 추가해 800×600 가로형 매장 디스플레이 이미지(좌측 키비주얼 + 우측 정보 패널)를 폼 입력만으로 JPG로 만든다. AI는 안 쓰고 정해진 틀에 데이터만 끼워넣는 방식.

**아키텍처(쉽게 말하면):**
- 새 모드 탭 "📌 POS/해피TV 화면 만들기" 추가 → 좌측은 입력 폼, 우측은 800×600 미리보기
- 미리보기는 AI 안 거치고 React가 직접 HTML 만들어 보여줌
- 7가지 블록(작은 라벨, 메인 카피, 강조 카피, 알약 라벨+값, 텍스트 줄, 등수 박스, QR+로고)을 자유롭게 추가/순서 변경
- 다 만들면 기존 JPG 내보내기 함수를 800×600 사이즈로 호출

**기술 스택:**
- React 18 + TypeScript + Vite (이미 있음)
- Zod (데이터 형식 검증)
- Zustand (상태 저장소, 이미 있음)
- html2canvas (JPG 내보내기, 이미 있음)
- 테스트는 비주얼 검증만 — 개발 서버에서 시안 5장 재현 확인

**참고 시안:** `c:\Users\admin\Downloads\POS\` 폴더의 5장 (밥먹고파바고, 가정의달, 파란장미, 저당말차, LAFC)

---

## 파일 구조 (만들 파일 / 고칠 파일)

| 경로 | 동작 | 책임 |
|---|---|---|
| `src/lib/posSchema.ts` | 신규 | POS 카드 데이터 형식 정의 (블록 배열 + 색·레이아웃 변수) |
| `src/lib/posSamples.ts` | 신규 | 시안 5장 샘플 데이터 |
| `src/lib/store.ts` | 수정 | POS 상태 + 액션(`setPosCard`, `addPosBlock` 등) 추가 |
| `src/components/posBlocks/*.tsx` | 신규 | 블록별 React 컴포넌트 (Eyebrow, Title, Highlight, PillRow, TextLine, RankList, QRBlock) |
| `src/components/POSCanvas.tsx` | 신규 | 800×600 정적 렌더링 컴포넌트 |
| `src/components/POSPreview.tsx` | 신규 | iframe 800×600 + POSCanvas 주입 |
| `src/components/POSMode.tsx` | 신규 | POS 입력 폼 패널 |
| `src/components/Editor.tsx` | 수정 | 탭 추가, mode 타입 확장 |
| `src/components/Preview.tsx` | 수정 | mode가 "pos"면 POSPreview 사용 |
| `src/lib/htmlExport.ts` | 수정 | `captureIframeAsJpeg`에 `width`·`height` 옵션 추가 |

---

## Task 1: POS 데이터 형식 정의

**파일:** 신규 `src/lib/posSchema.ts`

이 단계는 "POS 카드가 어떤 데이터로 이뤄져 있는지" Zod로 정의. 블록 7종 + 카드 전체 구조(`keyVisual` + `panel` + `blocks[]`).

- [ ] **단계 1.1: 블록별 zod 스키마 작성**

```typescript
// pb-builder/src/lib/posSchema.ts
import { z } from "zod";
import { nanoid } from "nanoid";

export const EyebrowBlockSchema = z.object({
  id: z.string().default(() => nanoid(8)),
  type: z.literal("eyebrow"),
  text: z.string().default(""),
});

export const TitleBlockSchema = z.object({
  id: z.string().default(() => nanoid(8)),
  type: z.literal("title"),
  lines: z.array(z.string()).default([""]),
});

export const HighlightBlockSchema = z.object({
  id: z.string().default(() => nanoid(8)),
  type: z.literal("highlight"),
  lines: z.array(z.string()).default([""]),
});

export const PillItemSchema = z.object({
  label: z.string(),
  value: z.string(),
});

export const PillRowBlockSchema = z.object({
  id: z.string().default(() => nanoid(8)),
  type: z.literal("pillRow"),
  items: z.array(PillItemSchema).default([]),
});

export const TextLineBlockSchema = z.object({
  id: z.string().default(() => nanoid(8)),
  type: z.literal("textLine"),
  text: z.string().default(""),
});

export const RankItemSchema = z.object({
  rank: z.number().int().min(1),
  text: z.string(),
});

export const RankListBlockSchema = z.object({
  id: z.string().default(() => nanoid(8)),
  type: z.literal("rankList"),
  items: z.array(RankItemSchema).default([]),
});

export const QRBlockSchema = z.object({
  id: z.string().default(() => nanoid(8)),
  type: z.literal("qrBlock"),
  qrDataUrl: z.string().default(""),
  caption: z.string().default(""),
});

export const POSBlockSchema = z.discriminatedUnion("type", [
  EyebrowBlockSchema,
  TitleBlockSchema,
  HighlightBlockSchema,
  PillRowBlockSchema,
  TextLineBlockSchema,
  RankListBlockSchema,
  QRBlockSchema,
]);

export const POSCardSchema = z.object({
  layout: z.enum(["split", "fullbleed"]).default("split"),
  keyVisualUrl: z.string().default(""),
  panelBg: z.string().default("#E8E5F2"),
  textPrimary: z.string().default("#1A1A1A"),
  textAccent: z.string().default("#3A4FB8"),
  pillBg: z.string().default("#3A4FB8"),
  pillText: z.string().default("#FFFFFF"),
  blocks: z.array(POSBlockSchema).default([]),
});

export type EyebrowBlock = z.infer<typeof EyebrowBlockSchema>;
export type TitleBlock = z.infer<typeof TitleBlockSchema>;
export type HighlightBlock = z.infer<typeof HighlightBlockSchema>;
export type PillItem = z.infer<typeof PillItemSchema>;
export type PillRowBlock = z.infer<typeof PillRowBlockSchema>;
export type TextLineBlock = z.infer<typeof TextLineBlockSchema>;
export type RankItem = z.infer<typeof RankItemSchema>;
export type RankListBlock = z.infer<typeof RankListBlockSchema>;
export type QRBlock = z.infer<typeof QRBlockSchema>;
export type POSBlock = z.infer<typeof POSBlockSchema>;
export type POSCard = z.infer<typeof POSCardSchema>;
```

- [ ] **단계 1.2: TypeScript 컴파일 검증**

```bash
cd pb-builder && npx tsc --noEmit
```

기대: 에러 0개.

- [ ] **단계 1.3: 커밋**

```bash
cd pb-builder && git add src/lib/posSchema.ts
git commit -m "feat: POS 카드 데이터 스키마 추가 (7개 블록 타입)"
```

---

## Task 2: 시안 5장 샘플 데이터

**파일:** 신규 `src/lib/posSamples.ts`

5장 시안 데이터를 미리 작성. 검증 + "샘플 불러오기" 버튼에서 사용.

- [ ] **단계 2.1: 가장 단순한 시안(밥먹고파바고) 작성**

```typescript
// pb-builder/src/lib/posSamples.ts
import type { POSCard } from "./posSchema";

export const sampleBabMeokGoPaba: POSCard = {
  layout: "split",
  keyVisualUrl: "",
  panelBg: "#F5EFD8",
  textPrimary: "#1A2D5A",
  textAccent: "#1A2D5A",
  pillBg: "#1A2D5A",
  pillText: "#FFFFFF",
  blocks: [
    { id: "b1", type: "title", lines: ["밥 먹고 파바 고?"] },
    { id: "b2", type: "highlight", lines: ["SET 최대 50% 혜택!"] },
    { id: "b3", type: "textLine", text: "1차 쿠폰 발급 : 4/6 ~ 4/10" },
    { id: "b4", type: "textLine", text: "2차 쿠폰 발급 : 4/11 ~ 4/15" },
    { id: "b5", type: "qrBlock", qrDataUrl: "", caption: "파바앱으로\n쿠폰 받으러 가기!" },
  ],
};

export const samplePosCards: Record<string, POSCard> = {
  bobMeokGoPaba: sampleBabMeokGoPaba,
};
```

- [ ] **단계 2.2: 나머지 4장 같은 패턴으로 추가**

각 시안 JPG (`c:\Users\admin\Downloads\POS\`)를 보면서:
- **가정의달케이크사전예약**: layout=split, panelBg=라벤더(#E8E5F2 근사), title 2줄 + highlight 2줄 + pillRow 2개("사전 예약"/"매장 픽업") + qrBlock
- **파란장미케이크오브제**: layout=split, panelBg=라벤더, eyebrow("파란장미 케이크 사전예약 하면") + highlight 2줄 + pillRow 2개 + qrBlock
- **저당말차케이크**: layout=split, panelBg=#4A6B3F 초록, eyebrow("파바앱 ONLY") + highlight 3줄 + pillRow 2개 + qrBlock
- **LAFC밋앤그릿**: layout=fullbleed, panelBg=#1E2218, title 2줄 + highlight + textLine 4줄 + rankList 4개 + qrBlock

각각 객체 5개 만들고 `samplePosCards`에 추가.

- [ ] **단계 2.3: 커밋**

```bash
cd pb-builder && git add src/lib/posSamples.ts
git commit -m "feat: POS 시안 5장 샘플 데이터 추가"
```

---

## Task 3: 블록별 렌더 컴포넌트 (7개)

**파일:** 신규 `src/components/posBlocks/{Eyebrow,Title,Highlight,PillRow,TextLine,RankList,QRBlock}.tsx` + `index.ts`

데이터를 받아서 JSX로 그리는 작은 컴포넌트들. 각 컴포넌트는 props만 받고 부수효과 없음.

- [ ] **단계 3.1: Eyebrow (작은 상단 라벨)**

```typescript
// posBlocks/Eyebrow.tsx
import type { EyebrowBlock } from "@/lib/posSchema";
type Props = { block: EyebrowBlock; color: string };
export const Eyebrow = ({ block, color }: Props) =>
  block.text ? (
    <div style={{ color, fontSize: 14, fontWeight: 700, letterSpacing: "-0.04em", marginBottom: 6 }}>
      {block.text}
    </div>
  ) : null;
```

- [ ] **단계 3.2: Title (메인 카피)**

```typescript
// posBlocks/Title.tsx
import type { TitleBlock } from "@/lib/posSchema";
type Props = { block: TitleBlock; color: string };
export const Title = ({ block, color }: Props) => (
  <div style={{ color, fontSize: 22, fontWeight: 700, letterSpacing: "-0.05em", lineHeight: 1.3, marginBottom: 10 }}>
    {block.lines.map((l, i) => <div key={i}>{l}</div>)}
  </div>
);
```

- [ ] **단계 3.3: Highlight (강조 카피)**

```typescript
// posBlocks/Highlight.tsx
import type { HighlightBlock } from "@/lib/posSchema";
type Props = { block: HighlightBlock; color: string };
export const Highlight = ({ block, color }: Props) => (
  <div style={{ color, fontSize: 24, fontWeight: 700, letterSpacing: "-0.05em", lineHeight: 1.3, marginBottom: 14 }}>
    {block.lines.map((l, i) => <div key={i}>{l}</div>)}
  </div>
);
```

- [ ] **단계 3.4: PillRow (알약 라벨 + 값)**

```typescript
// posBlocks/PillRow.tsx
import type { PillRowBlock } from "@/lib/posSchema";
type Props = { block: PillRowBlock; pillBg: string; pillText: string; textColor: string };
export const PillRow = ({ block, pillBg, pillText, textColor }: Props) => (
  <div style={{ display: "flex", flexDirection: "column", gap: 6, marginBottom: 12 }}>
    {block.items.map((item, i) => (
      <div key={i} style={{ display: "flex", alignItems: "center", gap: 8 }}>
        <span style={{
          background: pillBg, color: pillText,
          padding: "3px 12px", borderRadius: 999,
          fontSize: 12, fontWeight: 700, letterSpacing: "-0.04em",
        }}>{item.label}</span>
        <span style={{ color: textColor, fontSize: 13, fontWeight: 700 }}>{item.value}</span>
      </div>
    ))}
  </div>
);
```

- [ ] **단계 3.5: TextLine**

```typescript
// posBlocks/TextLine.tsx
import type { TextLineBlock } from "@/lib/posSchema";
type Props = { block: TextLineBlock; color: string };
export const TextLine = ({ block, color }: Props) => (
  <div style={{ color, fontSize: 13, fontWeight: 700, lineHeight: 1.6, marginBottom: 4 }}>
    {block.text}
  </div>
);
```

- [ ] **단계 3.6: RankList (등수 박스)**

```typescript
// posBlocks/RankList.tsx
import type { RankListBlock } from "@/lib/posSchema";
type Props = { block: RankListBlock; pillBg: string; pillText: string; textColor: string };
export const RankList = ({ block, pillBg, pillText, textColor }: Props) => (
  <div style={{ display: "flex", flexDirection: "column", gap: 6, marginBottom: 12 }}>
    {block.items.map((item) => (
      <div key={item.rank} style={{ display: "flex", alignItems: "center", gap: 8 }}>
        <span style={{
          width: 28, height: 28, borderRadius: 6,
          background: pillBg, color: pillText,
          fontSize: 11, fontWeight: 800,
          display: "flex", alignItems: "center", justifyContent: "center",
        }}>{item.rank}등</span>
        <span style={{ color: textColor, fontSize: 12, fontWeight: 700 }}>{item.text}</span>
      </div>
    ))}
  </div>
);
```

- [ ] **단계 3.7: QRBlock (QR + 로고 + 캡션, 항상 마지막)**

```typescript
// posBlocks/QRBlock.tsx
import type { QRBlock as QRBlockType } from "@/lib/posSchema";
type Props = { block: QRBlockType; logoUrl: string; textColor: string };
export const QRBlock = ({ block, logoUrl, textColor }: Props) => (
  <div style={{ display: "flex", alignItems: "flex-end", gap: 12, marginTop: "auto" }}>
    {block.qrDataUrl && <img src={block.qrDataUrl} alt="QR" style={{ width: 110, height: 110 }} />}
    <div style={{ display: "flex", flexDirection: "column", gap: 4 }}>
      {logoUrl && <img src={logoUrl} alt="PB" style={{ width: 36, height: 36 }} />}
      <div style={{ color: textColor, fontSize: 11, fontWeight: 700, lineHeight: 1.4, whiteSpace: "pre-line" }}>
        {block.caption}
      </div>
    </div>
  </div>
);
```

- [ ] **단계 3.8: barrel export**

```typescript
// posBlocks/index.ts
export * from "./Eyebrow";
export * from "./Title";
export * from "./Highlight";
export * from "./PillRow";
export * from "./TextLine";
export * from "./RankList";
export * from "./QRBlock";
```

- [ ] **단계 3.9: 컴파일 + 커밋**

```bash
cd pb-builder && npx tsc --noEmit
git add src/components/posBlocks/
git commit -m "feat: POS 블록 7종 React 컴포넌트 추가"
```

---

## Task 4: POSCanvas — 800×600 캔버스 컴포넌트

**파일:** 신규 `src/components/POSCanvas.tsx`

데이터 + 좌측 이미지를 받아서 800×600에 좌(이미지) / 우(블록 패널) 형태로 그리기. 블록 7종을 type별로 분기 렌더.

- [ ] **단계 4.1: POSCanvas 작성**

```typescript
// src/components/POSCanvas.tsx
import type { POSCard, POSBlock } from "@/lib/posSchema";
import { Eyebrow, Title, Highlight, PillRow, TextLine, RankList, QRBlock } from "./posBlocks";

type Props = { card: POSCard; logoUrl: string };

export const POSCanvas = ({ card, logoUrl }: Props) => {
  const renderBlock = (b: POSBlock) => {
    switch (b.type) {
      case "eyebrow": return <Eyebrow key={b.id} block={b} color={card.textPrimary} />;
      case "title": return <Title key={b.id} block={b} color={card.textPrimary} />;
      case "highlight": return <Highlight key={b.id} block={b} color={card.textAccent} />;
      case "pillRow": return <PillRow key={b.id} block={b} pillBg={card.pillBg} pillText={card.pillText} textColor={card.textPrimary} />;
      case "textLine": return <TextLine key={b.id} block={b} color={card.textPrimary} />;
      case "rankList": return <RankList key={b.id} block={b} pillBg={card.pillBg} pillText={card.pillText} textColor={card.textPrimary} />;
      case "qrBlock": return <QRBlock key={b.id} block={b} logoUrl={logoUrl} textColor={card.textPrimary} />;
    }
  };

  const isFullbleed = card.layout === "fullbleed";

  return (
    <div style={{
      width: 800, height: 600, position: "relative",
      background: isFullbleed && card.keyVisualUrl ? `url(${card.keyVisualUrl}) center/cover` : "#fff",
      fontFamily: "PBGothic, sans-serif",
    }}>
      <div style={{ display: "flex", width: "100%", height: "100%" }}>
        {!isFullbleed && (
          <div style={{
            width: "60%",
            background: card.keyVisualUrl ? `url(${card.keyVisualUrl}) center/cover` : "#f5f5f5",
          }} />
        )}
        <div style={{
          width: "40%",
          marginLeft: isFullbleed ? "auto" : 0,
          background: isFullbleed ? `${card.panelBg}E0` : card.panelBg,
          padding: "32px 28px",
          display: "flex",
          flexDirection: "column",
        }}>
          {card.blocks.map(renderBlock)}
        </div>
      </div>
    </div>
  );
};
```

- [ ] **단계 4.2: 컴파일 + 커밋**

```bash
cd pb-builder && npx tsc --noEmit
git add src/components/POSCanvas.tsx
git commit -m "feat: POS 캔버스 컴포넌트 (800x600 좌우 분할)"
```

---

## Task 5: store에 POS 상태 + 액션 추가

**파일:** 수정 `src/lib/store.ts`

- [ ] **단계 5.1: 현재 store.ts 전체 내용 확인**

```bash
cd pb-builder && cat src/lib/store.ts
```

zustand store 구조와 기존 액션 패턴 파악 후 같은 스타일로 추가.

- [ ] **단계 5.2: store에 추가**

```typescript
// 상단 import에 추가:
import { POSCardSchema, type POSCard, type POSBlock } from "./posSchema";

// state 인터페이스에:
posCard: POSCard;

// 초기값:
posCard: POSCardSchema.parse({}),

// 액션:
setPosCard: (card: POSCard) => set({ posCard: card }),
updatePosCardField: <K extends keyof POSCard>(key: K, value: POSCard[K]) =>
  set((state) => ({ posCard: { ...state.posCard, [key]: value } })),
addPosBlock: (block: POSBlock) =>
  set((state) => ({ posCard: { ...state.posCard, blocks: [...state.posCard.blocks, block] } })),
removePosBlock: (id: string) =>
  set((state) => ({ posCard: { ...state.posCard, blocks: state.posCard.blocks.filter(b => b.id !== id) } })),
movePosBlock: (id: string, direction: "up" | "down") =>
  set((state) => {
    const blocks = [...state.posCard.blocks];
    const idx = blocks.findIndex(b => b.id === id);
    if (idx === -1) return state;
    const target = direction === "up" ? idx - 1 : idx + 1;
    if (target < 0 || target >= blocks.length) return state;
    [blocks[idx], blocks[target]] = [blocks[target], blocks[idx]];
    return { posCard: { ...state.posCard, blocks } };
  }),
```

- [ ] **단계 5.3: 컴파일 + 커밋**

```bash
cd pb-builder && npx tsc --noEmit
git add src/lib/store.ts
git commit -m "feat: store에 POS 상태와 블록 조작 액션 추가"
```

---

## Task 6: POSPreview — iframe 800×600 + POSCanvas 주입

**파일:** 신규 `src/components/POSPreview.tsx`

미리보기를 iframe으로 만들고 그 안에 POSCanvas를 React로 렌더. 폰트 적용 위해 head에 @font-face 주입.

- [ ] **단계 6.1: 폰트 CSS 함수 확인**

```bash
cd pb-builder && cat src/lib/fontBase64.ts
```

기존에 `@font-face` 문자열을 반환하는 함수가 있는지 확인. 없으면 추가:

```typescript
// fontBase64.ts에 추가 (없을 경우)
export const getPBGothicCss = () => `
  @font-face {
    font-family: 'PBGothic';
    src: url(${PBGOTHIC_BD_BASE64}) format('truetype');
    font-weight: 700;
  }
`;
```

- [ ] **단계 6.2: POSPreview 작성**

```typescript
// src/components/POSPreview.tsx
import { forwardRef, useEffect, useRef, useState } from "react";
import ReactDOM from "react-dom/client";
import { useCampaignStore } from "@/lib/store";
import { getLogoDataUrl } from "@/lib/assets";
import { getPBGothicCss } from "@/lib/fontBase64";
import { POSCanvas } from "./POSCanvas";

export const POSPreview = forwardRef<HTMLIFrameElement>((_, ref) => {
  const card = useCampaignStore((s) => s.posCard);
  const [logoUrl, setLogoUrl] = useState("");
  const iframeRef = useRef<HTMLIFrameElement | null>(null);
  const rootRef = useRef<ReactDOM.Root | null>(null);

  useEffect(() => { getLogoDataUrl().then(setLogoUrl); }, []);

  useEffect(() => {
    const iframe = iframeRef.current;
    if (!iframe) return;
    const doc = iframe.contentDocument;
    if (!doc) return;

    if (!doc.getElementById("pos-style")) {
      doc.head.innerHTML = `
        <style id="pos-style">
          ${getPBGothicCss()}
          html, body { margin: 0; padding: 0; background: #f0f0f0; }
          body { display: flex; align-items: center; justify-content: center; min-height: 100vh; }
          #pos-root { width: 800px; height: 600px; box-shadow: 0 4px 20px rgba(0,0,0,0.1); }
        </style>
      `;
      doc.body.innerHTML = `<div id="pos-root"></div>`;
      const mountNode = doc.getElementById("pos-root")!;
      rootRef.current = ReactDOM.createRoot(mountNode);
    }

    rootRef.current?.render(<POSCanvas card={card} logoUrl={logoUrl} />);
  }, [card, logoUrl]);

  return (
    <iframe
      ref={(el) => {
        iframeRef.current = el;
        if (typeof ref === "function") ref(el);
        else if (ref) ref.current = el;
      }}
      title="POS Preview"
      style={{ width: "100%", height: "100%", border: "none", background: "#f0f0f0" }}
    />
  );
});

POSPreview.displayName = "POSPreview";
```

- [ ] **단계 6.3: 컴파일 + 커밋**

```bash
cd pb-builder && npx tsc --noEmit
git add src/components/POSPreview.tsx src/lib/fontBase64.ts
git commit -m "feat: POS 미리보기 iframe (800x600 고정 + 폰트 주입)"
```

---

## Task 7: POSMode — 좌측 입력 폼 패널

**파일:** 신규 `src/components/POSMode.tsx`

- [ ] **단계 7.1: 골격 작성 (레이아웃 + 키비주얼 + 컬러)**

```typescript
// src/components/POSMode.tsx
import { useCampaignStore } from "@/lib/store";
import { uploadImage } from "@/lib/imageUpload";
import { samplePosCards } from "@/lib/posSamples";
import type { POSBlock } from "@/lib/posSchema";
import { nanoid } from "nanoid";

export const POSMode = () => {
  const card = useCampaignStore((s) => s.posCard);
  const setCard = useCampaignStore((s) => s.setPosCard);
  const updateField = useCampaignStore((s) => s.updatePosCardField);
  const addBlock = useCampaignStore((s) => s.addPosBlock);
  const removeBlock = useCampaignStore((s) => s.removePosBlock);
  const moveBlock = useCampaignStore((s) => s.movePosBlock);

  const onUploadKV = async (file: File) => {
    const item = await uploadImage(file);  // 기존 함수 - {dataUrl, ...} 반환 가정. 시그니처 확인 필요.
    updateField("keyVisualUrl", item.dataUrl ?? (item as any));
  };

  const newBlock = (type: POSBlock["type"]): POSBlock => {
    const id = nanoid(8);
    switch (type) {
      case "eyebrow": return { id, type, text: "" };
      case "title": return { id, type, lines: [""] };
      case "highlight": return { id, type, lines: [""] };
      case "pillRow": return { id, type, items: [] };
      case "textLine": return { id, type, text: "" };
      case "rankList": return { id, type, items: [] };
      case "qrBlock": return { id, type, qrDataUrl: "", caption: "" };
    }
  };

  return (
    <div className="flex flex-col gap-4 p-4">
      <section>
        <h3 className="mb-2 text-sm font-bold">샘플 불러오기</h3>
        <div className="flex flex-wrap gap-1">
          {Object.entries(samplePosCards).map(([key, sample]) => (
            <button key={key} onClick={() => setCard(sample)} className="rounded border px-2 py-1 text-xs">{key}</button>
          ))}
        </div>
      </section>

      <section>
        <h3 className="mb-2 text-sm font-bold">레이아웃</h3>
        <select
          value={card.layout}
          onChange={(e) => updateField("layout", e.target.value as "split" | "fullbleed")}
          className="w-full rounded border px-2 py-1 text-sm"
        >
          <option value="split">분할 (좌 이미지 / 우 패널)</option>
          <option value="fullbleed">풀블리드 (전체 배경 + 우측 반투명 패널)</option>
        </select>
      </section>

      <section>
        <h3 className="mb-2 text-sm font-bold">키비주얼 이미지</h3>
        <input type="file" accept="image/*" onChange={(e) => e.target.files?.[0] && onUploadKV(e.target.files[0])} />
        {card.keyVisualUrl && <img src={card.keyVisualUrl} alt="" className="mt-2 max-h-32" />}
      </section>

      <section>
        <h3 className="mb-2 text-sm font-bold">컬러</h3>
        <div className="grid grid-cols-2 gap-2 text-xs">
          <label>패널 배경 <input type="color" value={card.panelBg} onChange={(e) => updateField("panelBg", e.target.value)} /></label>
          <label>본문 글자 <input type="color" value={card.textPrimary} onChange={(e) => updateField("textPrimary", e.target.value)} /></label>
          <label>강조 글자 <input type="color" value={card.textAccent} onChange={(e) => updateField("textAccent", e.target.value)} /></label>
          <label>알약 배경 <input type="color" value={card.pillBg} onChange={(e) => updateField("pillBg", e.target.value)} /></label>
        </div>
      </section>

      <section>
        <h3 className="mb-2 text-sm font-bold">블록 추가</h3>
        <div className="flex flex-wrap gap-1">
          {(["eyebrow","title","highlight","pillRow","textLine","rankList","qrBlock"] as const).map(t => (
            <button key={t} type="button" className="rounded border px-2 py-1 text-xs hover:bg-gray-50" onClick={() => addBlock(newBlock(t))}>
              + {t}
            </button>
          ))}
        </div>
      </section>

      <section>
        <h3 className="mb-2 text-sm font-bold">블록 ({card.blocks.length})</h3>
        <div className="flex flex-col gap-2">
          {card.blocks.map((b, i) => (
            <div key={b.id} className="rounded border p-2 text-xs">
              <div className="flex items-center justify-between">
                <span className="font-bold">{i + 1}. {b.type}</span>
                <div className="flex gap-1">
                  <button onClick={() => moveBlock(b.id, "up")} disabled={i === 0}>↑</button>
                  <button onClick={() => moveBlock(b.id, "down")} disabled={i === card.blocks.length - 1}>↓</button>
                  <button onClick={() => removeBlock(b.id)} className="text-red-600">×</button>
                </div>
              </div>
              <BlockEditor block={b} />
            </div>
          ))}
        </div>
      </section>
    </div>
  );
};
```

- [ ] **단계 7.2: BlockEditor — 블록 타입별 인라인 폼**

같은 파일 안. 각 타입에 맞는 입력 UI 분기.

```typescript
const BlockEditor = ({ block }: { block: POSBlock }) => {
  const update = (next: POSBlock) => {
    useCampaignStore.setState((s) => ({
      posCard: { ...s.posCard, blocks: s.posCard.blocks.map(b => b.id === block.id ? next : b) }
    }));
  };

  switch (block.type) {
    case "eyebrow":
    case "textLine":
      return (
        <input type="text" value={block.text}
          onChange={(e) => update({ ...block, text: e.target.value })}
          className="mt-1 w-full rounded border px-2 py-1 text-xs" />
      );
    case "title":
    case "highlight":
      return (
        <textarea value={block.lines.join("\n")} rows={3}
          onChange={(e) => update({ ...block, lines: e.target.value.split("\n") })}
          className="mt-1 w-full rounded border px-2 py-1 text-xs" />
      );
    case "pillRow":
      return (
        <div className="mt-1 flex flex-col gap-1">
          {block.items.map((item, i) => (
            <div key={i} className="flex gap-1">
              <input value={item.label} placeholder="라벨" onChange={(e) => {
                const items = [...block.items]; items[i] = { ...item, label: e.target.value };
                update({ ...block, items });
              }} className="w-1/3 rounded border px-1 text-xs" />
              <input value={item.value} placeholder="값" onChange={(e) => {
                const items = [...block.items]; items[i] = { ...item, value: e.target.value };
                update({ ...block, items });
              }} className="flex-1 rounded border px-1 text-xs" />
              <button onClick={() => update({ ...block, items: block.items.filter((_, j) => j !== i) })}>×</button>
            </div>
          ))}
          <button onClick={() => update({ ...block, items: [...block.items, { label: "", value: "" }] })} className="text-xs text-blue-600">+ 항목</button>
        </div>
      );
    case "rankList":
      return (
        <div className="mt-1 flex flex-col gap-1">
          {block.items.map((item, i) => (
            <div key={i} className="flex gap-1">
              <input type="number" value={item.rank} onChange={(e) => {
                const items = [...block.items]; items[i] = { ...item, rank: Number(e.target.value) };
                update({ ...block, items });
              }} className="w-12 rounded border px-1 text-xs" />
              <input value={item.text} onChange={(e) => {
                const items = [...block.items]; items[i] = { ...item, text: e.target.value };
                update({ ...block, items });
              }} className="flex-1 rounded border px-1 text-xs" />
              <button onClick={() => update({ ...block, items: block.items.filter((_, j) => j !== i) })}>×</button>
            </div>
          ))}
          <button onClick={() => update({ ...block, items: [...block.items, { rank: block.items.length + 1, text: "" }] })} className="text-xs text-blue-600">+ 등수</button>
        </div>
      );
    case "qrBlock":
      return (
        <div className="mt-1 flex flex-col gap-1">
          <input type="file" accept="image/*" onChange={async (e) => {
            const file = e.target.files?.[0]; if (!file) return;
            const dataUrl = await new Promise<string>((res) => {
              const r = new FileReader(); r.onload = () => res(r.result as string); r.readAsDataURL(file);
            });
            update({ ...block, qrDataUrl: dataUrl });
          }} />
          <textarea value={block.caption} rows={2} placeholder="캡션"
            onChange={(e) => update({ ...block, caption: e.target.value })}
            className="rounded border px-1 text-xs" />
        </div>
      );
  }
};
```

- [ ] **단계 7.3: 컴파일 + 커밋**

```bash
cd pb-builder && npx tsc --noEmit
git add src/components/POSMode.tsx
git commit -m "feat: POS 입력 폼 패널 (블록 추가/순서/편집)"
```

---

## Task 8: Editor + Preview에 POS 모드 끼워넣기

**파일:** 수정 `src/components/Editor.tsx`, `src/components/Preview.tsx`

- [ ] **단계 8.1: Editor.tsx mode 타입 확장 + 탭 추가**

```typescript
// 변경: type Mode = "auto" | "detail";
type Mode = "auto" | "detail" | "pos";
```

탭 영역에 추가:
```tsx
<button
  type="button"
  onClick={() => setMode("pos")}
  className={`border-b-2 px-6 py-2.5 text-sm ${
    mode === "pos" ? "border-theme font-bold text-theme" : "border-transparent text-fg-muted hover:text-fg"
  }`}
>
  📌 POS/해피TV 화면 만들기
</button>
```

aside 분기에 추가:
```tsx
{mode === "pos" ? (
  <POSMode />
) : mode === "auto" ? (
  <AutoMode onGenerated={() => {}} previewRef={previewRef} />
) : htmlOutput ? (
  <HtmlDetailMode previewRef={previewRef} />
) : (
  <DetailMode />
)}
```

상단 import에:
```typescript
import { POSMode } from "./POSMode";
```

- [ ] **단계 8.2: Preview.tsx에서 mode==="pos" 분기**

```typescript
import { POSPreview } from "./POSPreview";

// Preview 컴포넌트 안 첫 분기:
if (mode === "pos") {
  return <POSPreview ref={ref} />;
}
```

타입도 갱신: `type Props = { mode: "auto" | "detail" | "pos" };`

- [ ] **단계 8.3: 비주얼 검증**

```bash
cd pb-builder && npm run dev
```

브라우저(http://localhost:5173)에서:
- 📌 POS/해피TV 화면 만들기 탭 클릭 → 우측에 800×600 빈 캔버스
- 키비주얼 업로드 → 좌측 60% 영역에 이미지
- "+ title" → textarea 입력 → 우측 즉시 반영
- "샘플 불러오기 > bobMeokGoPaba" → 5개 블록 자동 채워짐

- [ ] **단계 8.4: 커밋**

```bash
cd pb-builder && git add src/components/Editor.tsx src/components/Preview.tsx
git commit -m "feat: Editor에 POS 모드 탭 추가, Preview 분기"
```

---

## Task 9: JPG 내보내기 800×600 사이즈 옵션

**파일:** 수정 `src/lib/htmlExport.ts`, `src/components/POSMode.tsx`

- [ ] **단계 9.1: 현재 captureIframeAsJpeg 시그니처 확인**

```bash
cd pb-builder && cat src/lib/htmlExport.ts
```

92번째 줄부터의 함수 본문 확인. 인자 형태 파악 후 옵션 추가.

- [ ] **단계 9.2: width/height/filename 옵션 추가**

```typescript
// htmlExport.ts 변경:
export const captureIframeAsJpeg = async (
  iframe: HTMLIFrameElement,
  options?: { width?: number; height?: number; filename?: string; targetSelector?: string }
): Promise<void> => {
  const { default: html2canvas } = await import("html2canvas");
  const doc = iframe.contentDocument;
  if (!doc) throw new Error("iframe document 없음");
  const target = options?.targetSelector
    ? doc.querySelector(options.targetSelector) as HTMLElement
    : doc.body;
  if (!target) throw new Error("캡처 대상 없음");
  const canvas = await html2canvas(target, {
    width: options?.width,
    height: options?.height,
    scale: 2,  // 2배 해상도 (선명도)
    useCORS: true,
    backgroundColor: null,
  });
  const dataUrl = canvas.toDataURL("image/jpeg", 0.92);
  const a = document.createElement("a");
  a.href = dataUrl;
  a.download = options?.filename ?? "pos.jpg";
  a.click();
};
```

> ⚠️ 기존 호출자(AutoMode 등)가 있으면 깨지지 않게 옵션을 모두 옵셔널로 유지.

- [ ] **단계 9.3: POSMode에 "JPG로 저장" 버튼**

POSMode가 previewRef를 받도록 prop 추가:

```typescript
// POSMode.tsx
type Props = { previewRef: React.RefObject<HTMLIFrameElement> };
export const POSMode = ({ previewRef }: Props) => {
  // ... 기존 코드
  const exportJpg = async () => {
    if (!previewRef.current) return;
    await captureIframeAsJpeg(previewRef.current, {
      width: 800, height: 600,
      filename: `pos-${Date.now()}.jpg`,
      targetSelector: "#pos-root",
    });
  };

  return (
    <div className="flex flex-col gap-4 p-4">
      {/* ... 기존 sections */}
      <button onClick={exportJpg} className="rounded bg-theme px-3 py-2 text-sm font-bold text-white">
        JPG로 저장
      </button>
    </div>
  );
};
```

Editor.tsx에서 `<POSMode previewRef={previewRef} />`로 prop 전달.

- [ ] **단계 9.4: 비주얼 검증**

```bash
cd pb-builder && npm run dev
```

- 샘플 "bobMeokGoPaba" 불러오기
- "JPG로 저장" 클릭
- 다운로드된 파일을 이미지 뷰어로 열어 800×600 (또는 2배 1600×1200), 폰트·컬러 OK인지 확인

- [ ] **단계 9.5: 커밋**

```bash
cd pb-builder && git add src/lib/htmlExport.ts src/components/POSMode.tsx src/components/Editor.tsx
git commit -m "feat: POS 800x600 JPG 내보내기"
```

---

## Task 10: 시안 5장 재현 검증

이 단계가 진짜 결승점. 실제 시안 5장을 폼에서 만들어 원본과 비교.

- [ ] **단계 10.1: 5장 각각 비교**

각 샘플 클릭 → JPG 저장 → `c:\Users\admin\Downloads\POS\` 원본과 옆에 두고 비교:
- [ ] 밥먹고파바고
- [ ] 가정의달케이크사전예약
- [ ] 파란장미케이크오브제
- [ ] 저당말차케이크
- [ ] LAFC밋앤그릿 (풀블리드 변형)

비교 항목:
- 폰트 굵기·크기 (PBGothicBd 700 weight)
- 자간 (-0.04~-0.05em)
- 컬러 매칭 (스포이드로 원본 픽업)
- 블록 간격
- 풀블리드 배경 처리 (LAFC만)

- [ ] **단계 10.2: 차이 나는 부분 수정**

`posBlocks/*.tsx` 또는 `POSCanvas.tsx`에서 폰트 크기·여백 조정. 반복:
1. 수정 → npm run dev 핫리로드
2. 같은 샘플 다시 JPG 저장
3. 원본과 비교
4. 차이 0에 수렴할 때까지

각 수정마다 작은 커밋:
```bash
git commit -m "fix: PillRow 알약 패딩 6px → 8px (가정의달 시안 재현)"
```

- [ ] **단계 10.3: 마무리 커밋 + 푸시**

```bash
cd pb-builder
git add .
git commit -m "feat: POS 시안 5장 재현 검증 완료"
git push
```

---

## Self-Review (계획서 자체 점검)

**1. 스펙 커버리지:**
- [x] 800×600 가로 JPG → Task 9
- [x] 좌측 키비주얼 + 우측 정보 패널 → Task 4 (POSCanvas)
- [x] 블록 7종 → Task 1 (스키마), Task 3 (렌더)
- [x] 컬러 변수 4개 → Task 1 (스키마), Task 7 (입력)
- [x] 풀블리드 변형 (LAFC) → Task 1 (layout enum), Task 4 (조건 분기)
- [x] PBGothicBd 폰트 → Task 6 (iframe head 주입)
- [x] 시안 5장 재현 → Task 2 (샘플), Task 10 (검증)

**2. 플레이스홀더 점검:**
- "기존 함수명 확인 후 맞춰" 표현이 Task 5, 6, 7, 9에 있음 — 실행자가 해당 파일 먼저 읽도록 명시. 허용.
- 시안 4장 샘플은 Task 2.2에서 "같은 패턴으로 추가" — 1장 풀 코드 보여줬고 컬러·블록 구성을 줄별로 적시. 허용.

**3. 타입 일관성:**
- 액션명: `setPosCard`, `addPosBlock`, `removePosBlock`, `movePosBlock`, `updatePosCardField` — Task 5와 7에서 동일.
- 카드 필드: `keyVisualUrl`, `panelBg`, `textPrimary`, `textAccent`, `pillBg`, `pillText` — Task 1·4·7 일치.
- 블록 type 문자열: `eyebrow|title|highlight|pillRow|textLine|rankList|qrBlock` — 일관.

---

## 실행 방식

1. **서브에이전트 방식 (추천)**: 매 Task마다 새 에이전트 → 사이에 리뷰 → 빠른 반복
2. **인라인 실행**: 이 세션에서 차례로 실행, 중간 체크포인트
